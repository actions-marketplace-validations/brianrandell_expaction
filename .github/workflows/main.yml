name: Test My Actions

on:
  workflow_dispatch:

jobs:
  test:
    name: Test My Action
    runs-on: 'ubuntu-latest'
    steps:
    - name: Run The Action
      uses: ./
      with: 
        greeting: 'Hello Good Sir!'
